# مكتبة خالد (Khalid Library) 📚

مشروع مكتبة إلكترونية متكامل يهدف إلى إدارة الكتب والمستخدمين، مع لوحة تحكم إدارية ونظام تسوق إلكتروني للكتب.

## ✨ المميزات الرئيسية

- **لوحة تحكم المدير (Admin Dashboard):** لإدارة الكتب، التصنيفات، والمستخدمين.
- **نظام المستخدمين:** تسجيل الدخول، إنشاء حساب، وإدارة الملف الشخصي.
- **تصفح الكتب:** عرض الكتب حسب التصنيف مع تفاصيل كاملة.
- **سلة التسوق:** إضافة الكتب إلى السلة وإدارة الطلبات.
- **إدارة المحتوى:** رفع صور الكتب وإدارة البيانات بسهولة.

## 🛠️ التقنيات المستخدمة (Tech Stack)

### الواجهة الأمامية (Frontend)
- **React.js:** بناء واجهة مستخدم تفاعلية.
- **Vite:** أداة بناء سريعة جداً.
- **Tailwind CSS:** لتنسيق الواجهة بتصميم عصري.
- **Framer Motion:** لإضافة حركات وانتقالات سلسة.
- **Lucide React:** أيقونات عصرية وبسيطة.

### الواجهة الخلفية (Backend)
- **Node.js & Express:** بناء الخادم وإدارة الـ API.
- **MongoDB & Mongoose:** قاعدة بيانات مستندات مرنة.
- **JWT (JSON Web Token):** لتأمين الوصول والمصادقة.
- **Bcrypt:** لتشفير كلمات المرور.
- **Multer:** لرفع وإدارة الصور.

---

## 🚀 طريقة التشغيل (Setup Guide)

### 📋 المتطلبات الأساسية
- تثبيت [Node.js](https://nodejs.org/)
- حساب [MongoDB Atlas](https://www.mongodb.com/cloud/atlas) أو تثبيت MongoDB محلياً.

### 1️⃣ إعداد الواجهة الخلفية (Backend)
1. افتح المجلد الرئيسي للمشروع في الطرفية (Terminal).
2. قم بتثبيت المكتبات المطلوبة:
   ```bash
   npm install
   ```
3. قم بإنشاء ملف `.env` في المجلد الرئيسي وأضف المتغيرات التالية (مثال):
   ```env
   PORT=5000
   MONGO_URI=your_mongodb_connection_string
   JWT_SECRET=your_jwt_secret
   ```
4. قم بتشغيل الخادم:
   ```bash
   npm run dev
   ```

### 2️⃣ إعداد الواجهة الأمامية (Frontend)
1. انتقل إلى مجلد الواجهة الأمامية:
   ```bash
   cd frontend
   ```
2. قم بتثبيت المكتبات:
   ```bash
   npm install
   ```
3. قم بتشغيل تطبيق React:
   ```bash
   npm run dev
   ```
4. سيفتح التطبيق عادةً على الرابط: `http://localhost:5173`

---

## 📂 هيكلة المشروع (Project Structure)

```text
khalid-library-v1.0/
├── frontend/          # كود الواجهة الأمامية (React + Vite)
├── backend/           # (اختياري) إذا كان كود السيرفر منفصل
├── models/            # نماذج قاعدة البيانات (Mongoose schemas)
├── routes/            # مسارات الـ API (Express routes)
├── config/            # إعدادات قاعدة البيانات والبيئة
├── images/            # الصور المرفوعة
├── server.js          # نقطة انطلاق الخادم الرئيسية
└── package.json       # إعدادات المشروع والمكتبات
```

## 📝 ملاحظات
- تأكد من تشغيل MongoDB قبل بدء تشغيل الخادم.
- الصور يتم تخزينها في مجلد `images` ويتم الوصول إليها عبر المسار الاستاتيكي في Express.

---
تم التطوير بواسطة: **سلمان العنزي**
